# Runtime dependencies used by Airflow DAG code.
requests>=2.33.0
psycopg2-binary>=2.9.11

# dbt CLI used by the GTFS static refresh DAG.
dbt-bigquery>=1.10.0
dbt-core>=1.10.20
dbt-postgres>=1.10.0

# GCS/BigQuery client libraries used by the hybrid GTFS loader.
google-cloud-bigquery[pandas]>=3.38.0
google-cloud-storage>=2.19.0,<3.2
