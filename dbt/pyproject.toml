[project]
name = "transit-reliability-dbt"
version = "0.1.0"
description = "dbt project dependencies for transit reliability analytics models"
requires-python = ">=3.11,<3.14"
dependencies = [
    "dbt-core>=1.10.20",
    "dbt-postgres>=1.10.0",
]
